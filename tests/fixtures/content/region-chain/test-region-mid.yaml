apiVersion: oscilla/v1
kind: Region
metadata:
  name: test-region-mid
spec:
  displayName: "Middle Region"
  description: "A region requiring level 2."
  parent: test-region-root
  unlock:
    type: level
    value: 2
