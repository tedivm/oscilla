apiVersion: oscilla/v1
kind: CharacterConfig
metadata:
  name: trigger-test-character-config
spec:
  public_stats:
    - name: strength
      type: int
      default: 10
      description: "Physical power"
    - name: gold
      type: int
      default: 0
      description: "Gold coins"
    - name: xp
      type: int
      default: 0
      bounds:
        min: 0
      description: "Experience points"
  hidden_stats: []
