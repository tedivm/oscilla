apiVersion: game/v1
kind: Adventure
metadata:
  name: test-ungated
spec:
  displayName: "No Gate"
  description: "No conditions required."
  steps:
    - type: narrative
      text: "No gate needed."
