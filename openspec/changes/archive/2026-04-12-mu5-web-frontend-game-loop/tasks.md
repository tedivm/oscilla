# Tasks: MU5 — Web Frontend — Game Loop

## 1. API Client Extensions

- [x] 1.1 In `frontend/src/lib/api/types.ts`, add the following interfaces after `CharacterStateRead`: `AdventureOptionRead { ref: string; display_name: string; description: string }`, `LocationOptionRead { ref: string; display_name: string; is_current: boolean }`, `RegionGraphNode { id: string; label: string; kind: string }`, `RegionGraphEdge { source: string; target: string; label: string }`, `RegionGraphRead { nodes: RegionGraphNode[]; edges: RegionGraphEdge[] }`, `OverworldStateRead { character_id: string; current_location: string | null; current_location_name: string | null; current_region_name: string | null; available_adventures: AdventureOptionRead[]; navigation_options: LocationOptionRead[]; region_graph: RegionGraphRead }`, `PendingStateRead { character_id: string; pending_event: Record<string, unknown> | null; session_output: Record<string, unknown>[] }`

- [x] 1.2 In `frontend/src/lib/api/characters.ts`, add `navigateLocation(characterId: string, locationRef: string): Promise<OverworldStateRead>` calling `apiFetch<OverworldStateRead>` with `POST /characters/{id}/navigate` and body `{ location_ref: locationRef }`; add `getOverworld(characterId: string): Promise<OverworldStateRead>` calling `apiFetch<OverworldStateRead>` with `GET /characters/{id}/overworld`

- [x] 1.3 Create `frontend/src/lib/api/play.ts` containing: (a) `AdvanceDecision` interface with optional fields `choice?: number`, `ack?: boolean`, `text_input?: string`, `skill_choice?: number` mirroring backend `AdvanceRequest`; (b) `CurrentPlayState` interface with `narrativeLog: NarrativeEntry[]`, `pendingEvent: SSEEvent | null`, `overworldState: OverworldStateRead | null`; (c) `getCurrentPlayState(characterId: string): Promise<CurrentPlayState>` that calls `apiFetch<PendingStateRead>` on `/characters/{id}/play/current`, reconstructs `narrativeLog` by filtering `session_output` for `type === "narrative"` entries and mapping to `NarrativeEntry`, and when `pendingEvent` is null also calls `getOverworld(characterId)` to populate `overworldState`; (d) `fetchSSE(url: string, body: object): AsyncGenerator<SSEEvent>` that reads the access token from `get(authStore)`, sends a `POST` with `Content-Type: application/json` and `Authorization: Bearer` header, throws `new ApiError("Session conflict", 409, "session_conflict")` on 409, throws `new ApiError(...)` on any other non-ok status, then reads `response.body` as a `ReadableStream` and yields events via `parseSSEBuffer`; (e) `parseSSEBuffer(buffer: string): { events: SSEEvent[]; remaining: string }` that splits on `\n\n`, parses `event:` and `data:` lines per block, and JSON-parses each `data` value

- [x] 1.4 Verify that `vite.config.ts` proxy already covers the new endpoints — `/characters` prefix covers `/characters/{id}/play/*`, `/characters/{id}/navigate`, and `/characters/{id}/overworld`; no proxy changes are required but confirm this with a test run of `npm run dev` against a live backend

## 2. `gameSession` Store

- [x] 2.1 Create `frontend/src/lib/stores/gameSession.ts` exporting: `SSEEventType` union type (`"narrative" | "combat_state" | "choice" | "ack_required" | "text_input" | "skill_menu" | "adventure_complete" | "error"`), `NarrativeEntry { id: string; text: string }`, `SSEEvent { type: SSEEventType; data: unknown }`, `AdvanceDecision` (re-export from `api/play.ts`), `GameSessionState { mode: "idle" | "loading" | "adventure" | "overworld" | "complete"; narrativeLog: NarrativeEntry[]; pendingEvent: SSEEvent | null; completeEvent: SSEEvent | null; overworldState: OverworldStateRead | null; error: string | null }`, and `gameSession` created via a `createGameSession()` factory function

- [x] 2.2 Implement `createGameSession()` using `writable<GameSessionState>` (not Svelte 5 runes — must be consumable from `.ts` files and `+layout.ts`); the returned object SHALL expose: `subscribe`, `init(playState: CurrentPlayState): void` (sets mode + populates log, pendingEvent, overworldState; derives mode as `"adventure"` when `pendingEvent !== null`, else `"overworld"`), `begin(characterId: string, adventureRef: string): Promise<void>`, `advance(characterId: string, decision: AdvanceDecision): Promise<void>`, `close(): void` (cancels the active generator, sets mode to `"idle"`); an internal `runStream(gen: AsyncGenerator<SSEEvent>)` that sets mode to `"loading"`, drains the generator calling `applyEvent()` per event, and catches errors into `state.error`; a `activeGenerator` reference that is nulled out by `close()` and compared against inside `runStream` to abort superseded streams

- [x] 2.3 Implement `applyEvent(s: GameSessionState, event: SSEEvent): GameSessionState` as a pure function (not a store method): `"narrative"` → append to `narrativeLog` with `id: crypto.randomUUID()`; `"adventure_complete"` → set `mode: "complete"` and `completeEvent`; `"error"` → set `mode: "overworld"` and `error`; all decision event types (`"combat_state"`, `"choice"`, `"ack_required"`, `"text_input"`, `"skill_menu"`) → set `pendingEvent` and `mode: "adventure"`

## 3. Game Components

- [x] 3.1 Create `frontend/src/lib/components/Game/NarrativeLog.svelte` accepting `entries: NarrativeEntry[]`; render a scrolling container with one `NarrativeEntry` per item; auto-scroll to bottom on new entries unless the player has manually scrolled up; show a "Scroll to bottom" button when scroll is not at the bottom; use a `$effect` (Svelte 5) or reactive statement to scroll when `entries.length` changes

- [x] 3.2 Create `frontend/src/lib/components/Game/NarrativeEntry.svelte` accepting `entry: NarrativeEntry`; render as a `<p>` with a CSS `opacity: 0 → 1` fade-in animation (300ms) applied once on mount; use `{entry.id}` as the `key` in the parent `{#each}` block so Svelte does not re-animate existing entries

- [x] 3.3 Create `frontend/src/lib/components/Game/ChoiceMenu.svelte` accepting `event: SSEEvent` (data typed as `{ prompt: string; options: string[] }`) and `onSelect: (choice: number) => void`; render the `prompt` text and one `Button` per option labeled with the option text; call `onSelect(i + 1)` (1-based) on button click; register `<svelte:window onkeydown>` to map keypresses `1`–`9` to `onSelect(n)` for `options[0]`–`options[8]`; disable all buttons while awaiting the next stream event (derive a `submitting` state that is set to `true` on selection)

- [x] 3.4 Create `frontend/src/lib/components/Game/AckPrompt.svelte` accepting `onAck: () => void`; render a "Press Enter to continue" button; register `<svelte:window onkeydown>` to call `onAck()` on `Enter` or `Space`; disable the button while awaiting

- [x] 3.5 Create `frontend/src/lib/components/Game/CombatHUD.svelte` accepting `event: SSEEvent` (data typed as `{ round: number; combatants: Array<{ name: string; hp: number; max_hp: number; is_player: boolean }> }`) and `onAck: () => void`; render the round counter, one HP bar per combatant (using a `<progress>` element with `value={hp}` and `max={max_hp}`), and a "Continue" button that calls `onAck()`; register `Enter`/`Space` keyboard shortcut via `<svelte:window onkeydown>`

- [x] 3.6 Create `frontend/src/lib/components/Game/TextInputForm.svelte` accepting `event: SSEEvent` (data typed as `{ prompt: string }`) and `onSubmit: (text: string) => void`; render the prompt text, a text `<input>`, and a Submit `Button`; on form submit call `onSubmit(inputValue.trim())`; validate that input is not empty; disable the input and button while awaiting

- [x] 3.7 Create `frontend/src/lib/components/Game/SkillMenu.svelte` accepting `event: SSEEvent` (data typed as `{ skills: Array<{ id: string; name: string; description: string; on_cooldown: boolean }> }`) and `onSelect: (skillIndex: number) => void`; render skill cards with `name`, `description`, and a cooldown indicator when `on_cooldown` is `true`; call `onSelect(i + 1)` (1-based) on card selection; disable cooled-down skills; number keys `1`–`9` map to `onSelect(n)` via `<svelte:window onkeydown>`

- [x] 3.8 Create `frontend/src/lib/components/Game/AdventureCompleteScreen.svelte` accepting `event: SSEEvent | null` (data typed as `{ outcome: string; narrative: string }`) and `onContinue: () => void`; render the `outcome` prominently, the `narrative` text below it, and a "Return to Overworld" button that calls `onContinue()`; handle `event === null` gracefully (do not crash)

- [x] 3.9 Create `frontend/src/lib/components/Game/SessionConflictModal.svelte` accepting `acquiredAt: string` (ISO 8601 timestamp as returned in the 409 `SessionConflictRead.acquired_at` field), `onTakeover: () => void`, and `onCancel: () => void`; render the lock age as a human-readable relative time (e.g., "Active since 3 minutes ago"); provide "Take over this session" button calling `onTakeover()` and a "Cancel" button calling `onCancel()`; render as a non-blocking overlay positioned above the overworld (not full-screen)

## 4. Overworld Components

- [x] 4.1 Create `frontend/src/lib/components/Overworld/OverworldView.svelte` accepting `characterId: string`, `state: OverworldStateRead | null`, and `onBeginAdventure: (adventureRef: string) => void`; assemble `LocationInfo`, `AdventureList`, `NavigationPanel`, and `CharacterSidebar` in a two-column layout (narrative column + sidebar); when `state` is null show a `LoadingSpinner`; poll `getOverworld(characterId)` every 5 seconds to detect triggered adventures and update state; when the polled result has a new `pending_event`, call `gameSession.begin(characterId, adventureRef)` directly to transition to adventure mode without navigating

- [x] 4.2 Create `frontend/src/lib/components/Overworld/LocationInfo.svelte` accepting `locationName: string | null`, `regionName: string | null`, and `description: string | null`; render location name as a heading, region name as subheading, and description when present; show a placeholder "Unknown location" when both are null

- [x] 4.3 Create `frontend/src/lib/components/Overworld/AdventureList.svelte` accepting `adventures: AdventureOptionRead[]` and `onSelect: (adventureRef: string) => void`; render each adventure as a card with `display_name` and `description`; show a "Begin" button per card that calls `onSelect(adventure.ref)`; show an empty-state "No adventures available here" when the list is empty

- [x] 4.4 Create `frontend/src/lib/components/Overworld/NavigationPanel.svelte` accepting `characterId: string`, `locations: LocationOptionRead[]`, and `onNavigated: (newState: OverworldStateRead) => void`; render a list of destination buttons; on button click, call `navigateLocation(characterId, location.ref)` from `api/characters.ts`, show a `LoadingSpinner` inline while the call is in progress, and call `onNavigated(newOverworldState)` on success; highlight the card for the current location (`is_current === true`) but disable its button; show `ErrorBanner` on `ApiError`

- [x] 4.5 Create `frontend/src/lib/components/Overworld/CharacterSidebar.svelte` accepting `character: CharacterStateRead`; render `name`, a compact stat list (only `hp`/`max_hp` if present, plus any stats with `display_name` set), `active_buffs` count, and `prestige_count`; keep the component small — full stats are on the character sheet page

- [x] 4.6 Create `frontend/src/lib/components/Overworld/InventoryQuickActions.svelte` as a **read-only placeholder** in MU5; accept `instances: ItemInstanceRead[]` and `stacks: Record<string, StackedItemRead>`; render a compact list of equipped item refs; add a `<!-- TODO MU6: equip/use actions -->` comment where action buttons will go; no click handlers needed

## 5. Play Route

- [x] 5.1 Create `frontend/src/routes/characters/[id]/play/+page.ts` as the load function: import `getCharacter` from `$lib/api/characters.ts` and `getCurrentPlayState` from `$lib/api/play.ts`; run both in parallel with `Promise.all`; return `{ character, playState }`; do not pass a `fetch` parameter to either function — they use `apiFetch` directly which reads `get(authStore)` at call time; add a comment explaining why the SvelteKit `fetch` param is not used (CSR-only app, store is populated before load runs)

- [x] 5.2 Create `frontend/src/routes/characters/[id]/play/+page.svelte` structured as follows: (a) call `gameSession.init(data.playState)` synchronously before render; (b) derive `characterId = data.character.id`; (c) declare `let showConflictModal = $state(false)` and `let conflictAcquiredAt = $state<string>("")`; (d) implement `handleChoice(choice: number)`, `handleAck()`, `handleTextInput(text: string)`, `handleSkillChoice(skillIndex: number)` — each calls the matching `gameSession.advance(characterId, { ... })` method with the correctly typed `AdvanceDecision` field populated; (e) implement `handleAdventureComplete()` that calls `goto('/characters/${characterId}')` after `gameSession.close()`; (f) catch `ApiError` with status `409` inside each handler, extract `acquiredAt` from the error body, set `conflictAcquiredAt` and `showConflictModal = true`; (g) implement `handleTakeover()` that calls `POST /characters/{id}/play/takeover` via `apiFetch` and resumes by calling the appropriate `gameSession.advance` or `gameSession.begin`; (h) register `<svelte:window onkeydown={handleGlobalKey} />` where `handleGlobalKey` delegates to the active component's shortcut handler; (i) call `gameSession.close()` in `onDestroy`; (j) render `OverworldView` when `$gameSession.mode === 'overworld'`, otherwise render `NarrativeLog` + `DecisionArea` as per the `{#if}` block in design.md; (k) conditionally render `SessionConflictModal` when `showConflictModal === true`

## 6. Navigation Guards

- [x] 6.1 Create `frontend/src/routes/characters/[id]/+layout.ts` (the D8 forced-redirect guard): import `apiFetch` from `$lib/api/client.ts` and `PendingStateRead` from `$lib/api/types.ts`; export `load: LayoutLoad` that calls `apiFetch<PendingStateRead>("/characters/${params.id}/play/current")` and throws `redirect(307, ...)` to the play page when `pending_event` is non-null; return `{}` when no adventure is active; add a comment explaining this prevents navigating to the character sheet while an adventure is in progress (D8)

- [x] 6.2 In `frontend/src/routes/+layout.svelte` (the root app layout), add a `beforeNavigate` import from `$app/navigation`; add the D7 hard-block guard: when `from?.url.pathname.includes('/play')` and `!to?.url.pathname.includes('/play')` and `$gameSession.mode === 'adventure' || $gameSession.mode === 'loading'`, call `cancel()`; add a comment explaining the hard-cancel rationale from D7 (prevents bypassing triggered adventures via browser navigation)

## 7. Testing

- [x] 7.1 Create `frontend/src/lib/stores/gameSession.test.ts` with source-contract tests (using `?raw` Vite import): (a) `gameSession.ts` exports `createGameSession`, `applyEvent`, `SSEEventType`; (b) the `narrativeLog` entry produced by an `"narrative"` event has `id` and `text` fields; (c) `applyEvent` for a decision event sets `mode: "adventure"` and `pendingEvent`; (d) `applyEvent` for `"adventure_complete"` sets `mode: "complete"` and `completeEvent`; (e) `applyEvent` for `"error"` sets `mode: "overworld"` and `error`

- [x] 7.2 Create `frontend/src/lib/api/play.test.ts` with source-contract tests: (a) `play.ts` imports `apiFetch` from `client.ts` (not raw `fetch`); (b) `play.ts` exports `fetchSSE`, `getCurrentPlayState`, `parseSSEBuffer`; (c) `parseSSEBuffer` with a complete two-block string returns two events and an empty `remaining`; (d) `parseSSEBuffer` with a partial block (no trailing `\n\n`) returns no events and preserves the input as `remaining`; (e) `fetchSSE` throws when `response.status === 409`

- [x] 7.3 Create `frontend/src/routes/characters/[id]/play/play.test.ts` with source-contract tests on `+page.svelte?raw`: (a) the page imports `gameSession` from `$lib/stores/gameSession`; (b) the page calls `gameSession.init(data.playState)` before rendering; (c) the page uses `beforeNavigate` or the D7 import pattern is visible in content; (d) `handleChoice` contains `gameSession.advance`; (e) `gameSession.close()` is called in `onDestroy`

- [x] 7.4 Create `frontend/tests/e2e/game/play.spec.ts` (future Playwright E2E — mark as `test.skip`): stub tests for: full adventure cycle from overworld → begin → make choices → complete → return to character sheet; crash recovery (reload mid-adventure restores log + pending decision); session conflict modal appears on 409; navigation guard hard-blocks mid-adventure; triggered adventure forces redirect from character sheet; `OverworldView` poll fires and transitions to adventure mode without navigation; add a `TODO: these require a live stack and will be unskipped in a dedicated E2E phase` comment block

## 8. Documentation

- [x] 8.1 Update `docs/dev/frontend.md` to add a "Game Loop" section covering: (a) `gameSession` store lifecycle (`init`, `begin`, `advance`, `close`) with a short code example; (b) `fetchSSE` generator and `parseSSEBuffer` — why `fetch` + `ReadableStream` is used instead of `EventSource` (POST-only requirement); (c) `getCurrentPlayState` dual-fetch design (`/play/current` + conditional `/overworld`); (d) why `writable` is used instead of Svelte 5 runes (cross-boundary `.ts` module access); (e) D7 navigation hard-block — the `beforeNavigate cancel()` pattern and why a confirmation dialog was rejected; (f) D8 forced redirect — the character-scoped `+layout.ts` redirect and the `OverworldView` poll for triggered adventures; (g) decision component callback prop pattern (Svelte 5 `$props()`, no `createEventDispatcher`); (h) keyboard shortcut registration with `<svelte:window onkeydown>` scoped to mounted components

- [x] 8.2 Update `docs/dev/game-engine.md` to add a note that the backend MUST emit a `event: stream-end\ndata: {}\n\n` SSE line after each decision pause event, and after `adventure_complete`, to signal clean stream closure to the `fetchSSE` generator; document all SSE event types and their JSON `data` field shapes that the TypeScript consumer now depends on

- [x] 8.3 Update `docs/authors/adventures.md` to add a brief note in the existing tutorial section that the browser interface renders all adventure decision types (narrative, choice, ack_required, combat_state, text_input, skill_menu) and that all per-type content fields (prompts, option labels, combat combatant names) appear exactly as specified in the manifest

## 9. Testlandia Content

- [x] 9.1 Add a `web-overworld-nav` adventure to `content/testlandia/regions/api-test/locations/api-hub/adventures/web-overworld-nav.yaml`: a short narrative-only adventure (2 narrative steps + 1 ack_required) with `displayName: "Overworld Nav Test"` and `description: "Completes immediately to return you to the overworld, testing the adventure_complete → overworld transition."`; add this adventure ref to `api-hub.yaml` `adventures` list

- [x] 9.2 Add a `text-input-adventure` adventure to `content/testlandia/regions/api-test/locations/api-hub/adventures/text-input-adventure.yaml`: one `narrative` step, one `text_input` step with `prompt: "Enter any text to continue:"`, one `narrative` step confirming the input was recorded; `displayName: "Text Input Test"`; add to `api-hub.yaml`

- [x] 9.3 Update `content/testlandia/regions/api-test/locations/api-hub/api-hub.yaml` to reference the two new adventures from 9.1 and 9.2 in the `adventures` list

- [x] 9.4 Verify the `api-secondary` location has no adventures listed (confirmed in existing YAML) — it serves as the navigation destination target for manual QA of the `NavigationPanel` → `navigateLocation` flow; no changes needed but confirm it validates correctly with `oscilla content validate`

- [x] 9.5 Run full content validation with `oscilla content validate testlandia` to confirm all new manifests parse correctly; run `oscilla content test testlandia` to confirm all adventures in the api-test region execute without errors in the Python engine
